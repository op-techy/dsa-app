services:
  postgres:
    image: postgres:16
    container_name: dsa_postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - dsa_postgres_data:/var/lib/postgresql/data

volumes:
  dsa_postgres_data: